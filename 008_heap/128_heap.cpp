#include"bits/stdc++.h"
using namespace std;
//* heap is like a binary tree
int main()
{
    //* two types of heap minHeap and maxHeap
    // is the root is maximum then it is max heap otherwise min heap
    return 0;
}